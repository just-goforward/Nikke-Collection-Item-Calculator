name: Deploy Forecast Collector Staging

on:
  push:
    branches: [main]
    paths:
      - "forecast-collector/**"
      - "forecast-dispatcher/**"
      - "forecast-interactions/**"
      - "shared/boundedHttp.ts"
      - "shared/d1QuotaEvidence.ts"
      - "shared/supplyForecastCandidate.ts"
      - "shared/supplyForecastModel.ts"
      - "scripts/smoke-forecast-collector.ts"
      - "scripts/smoke-forecast-dispatcher.ts"
      - "scripts/smoke-forecast-interactions.ts"
      - "scripts/send-forecast-router-test.ts"
      - "scripts/resolve-forecast-manual-review.ts"
      - "scripts/cloudflare-cron-capacity.ts"
      - "scripts/cloudflare-d1-budget.ts"
      - "scripts/lib/d1-budget.ts"
      - "scripts/probe-forecast-github-app.ts"
      - "scripts/forecast-dispatcher-workflow.spec.ts"
      - "vitest.forecast-collector.config.ts"
      - "vitest.forecast-dispatcher.config.ts"
      - "vitest.forecast-interactions.config.ts"
      - ".github/workflows/forecast-collector-deploy.yml"
      - ".github/workflows/forecast-d1-budget-watch.yml"
      - ".github/workflows/forecast-proposal.yml"
      - ".github/workflows/resolve-forecast-manual-review.yml"
      - "package.json"
      - "package-lock.json"
  workflow_dispatch:
    inputs:
      poll_mode:
        description: Naver poll mode for a fresh canary
        required: true
        default: both
        type: choice
        options:
          - both
          - alternating
      router_endpoint_ready:
        description: Discord Developer Portal now points to the dedicated Router URL
        required: true
        default: false
        type: boolean

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  group: forecast-collector-staging
  cancel-in-progress: false

jobs:
  deploy-staging:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout trusted main
        uses: actions/checkout@v7
        with:
          ref: refs/heads/main

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24.20.0
          cache: npm

      - name: Validate configuration
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          FORECAST_COLLECTOR_ADMIN_TOKEN: ${{ secrets.FORECAST_COLLECTOR_ADMIN_TOKEN }}
          FORECAST_COLLECTOR_STAGING_URL: ${{ vars.FORECAST_COLLECTOR_STAGING_URL }}
          FORECAST_INTERACTIONS_URL: ${{ vars.FORECAST_INTERACTIONS_URL }}
          FORECAST_GITHUB_APP_ID: ${{ vars.FORECAST_GITHUB_APP_ID }}
          FORECAST_GITHUB_APP_INSTALLATION_ID: ${{ vars.FORECAST_GITHUB_APP_INSTALLATION_ID }}
          FORECAST_GITHUB_APP_PRIVATE_KEY: ${{ secrets.FORECAST_GITHUB_APP_PRIVATE_KEY }}
          DISCORD_FORECAST_BOT_TOKEN: ${{ secrets.DISCORD_FORECAST_BOT_TOKEN }}
          DISCORD_FORECAST_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_CHANNEL_ID }}
          DISCORD_FORECAST_APPROVAL_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_APPROVAL_CHANNEL_ID }}
          DISCORD_FORECAST_ACTIVITY_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_ACTIVITY_CHANNEL_ID }}
          DISCORD_FORECAST_ALERT_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_ALERT_CHANNEL_ID }}
          DISCORD_FORECAST_FALLBACK_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_FALLBACK_CHANNEL_ID }}
          DISCORD_FORECAST_GUILD_ID: ${{ vars.DISCORD_FORECAST_GUILD_ID }}
          DISCORD_FORECAST_APPROVER_USER_ID: ${{ vars.DISCORD_FORECAST_APPROVER_USER_ID }}
          DISCORD_FORECAST_APPLICATION_ID: ${{ vars.DISCORD_FORECAST_APPLICATION_ID }}
          DISCORD_FORECAST_PUBLIC_KEY: ${{ vars.DISCORD_FORECAST_PUBLIC_KEY }}
        run: |
          test -n "$CLOUDFLARE_ACCOUNT_ID" || { echo "Missing CLOUDFLARE_ACCOUNT_ID variable." >&2; exit 1; }
          test -n "$CLOUDFLARE_API_TOKEN" || { echo "Missing CLOUDFLARE_API_TOKEN secret." >&2; exit 1; }
          test -n "$FORECAST_COLLECTOR_ADMIN_TOKEN" || { echo "Missing FORECAST_COLLECTOR_ADMIN_TOKEN secret." >&2; exit 1; }
          test -n "$FORECAST_COLLECTOR_STAGING_URL" || { echo "Missing FORECAST_COLLECTOR_STAGING_URL variable." >&2; exit 1; }
          if [[ "${{ github.event_name == 'workflow_dispatch' && inputs.router_endpoint_ready }}" == "true" ]]; then
            test -n "$FORECAST_INTERACTIONS_URL" || {
              echo "FORECAST_INTERACTIONS_URL is required when router_endpoint_ready=true." >&2
              exit 1
            }
          fi
          [[ "$FORECAST_GITHUB_APP_ID" =~ ^[0-9]{1,20}$ ]] || { echo "Missing or invalid FORECAST_GITHUB_APP_ID variable." >&2; exit 1; }
          [[ "$FORECAST_GITHUB_APP_INSTALLATION_ID" =~ ^[0-9]{1,20}$ ]] || { echo "Missing or invalid FORECAST_GITHUB_APP_INSTALLATION_ID variable." >&2; exit 1; }
          test -n "$FORECAST_GITHUB_APP_PRIVATE_KEY" || { echo "Missing FORECAST_GITHUB_APP_PRIVATE_KEY secret." >&2; exit 1; }
          test -n "$DISCORD_FORECAST_BOT_TOKEN" || { echo "Missing DISCORD_FORECAST_BOT_TOKEN secret." >&2; exit 1; }
          [[ "$DISCORD_FORECAST_CHANNEL_ID" =~ ^[0-9]{6,24}$ ]] || { echo "Missing or invalid DISCORD_FORECAST_CHANNEL_ID variable." >&2; exit 1; }
          [[ "$DISCORD_FORECAST_APPROVAL_CHANNEL_ID" =~ ^[0-9]{6,24}$ ]] || { echo "Missing or invalid DISCORD_FORECAST_APPROVAL_CHANNEL_ID variable." >&2; exit 1; }
          [[ "$DISCORD_FORECAST_ACTIVITY_CHANNEL_ID" =~ ^[0-9]{6,24}$ ]] || { echo "Missing or invalid DISCORD_FORECAST_ACTIVITY_CHANNEL_ID variable." >&2; exit 1; }
          [[ "$DISCORD_FORECAST_ALERT_CHANNEL_ID" =~ ^[0-9]{6,24}$ ]] || { echo "Missing or invalid DISCORD_FORECAST_ALERT_CHANNEL_ID variable." >&2; exit 1; }
          [[ "$DISCORD_FORECAST_FALLBACK_CHANNEL_ID" =~ ^[0-9]{6,24}$ ]] || { echo "Missing or invalid DISCORD_FORECAST_FALLBACK_CHANNEL_ID variable." >&2; exit 1; }
          [[ "$DISCORD_FORECAST_GUILD_ID" =~ ^[0-9]{6,24}$ ]] || { echo "Missing or invalid DISCORD_FORECAST_GUILD_ID variable." >&2; exit 1; }
          [[ "$DISCORD_FORECAST_APPROVER_USER_ID" =~ ^[0-9]{6,24}$ ]] || { echo "Missing or invalid DISCORD_FORECAST_APPROVER_USER_ID variable." >&2; exit 1; }
          [[ "$DISCORD_FORECAST_APPLICATION_ID" =~ ^[0-9]{6,24}$ ]] || { echo "Missing or invalid DISCORD_FORECAST_APPLICATION_ID variable." >&2; exit 1; }
          [[ "$DISCORD_FORECAST_PUBLIC_KEY" =~ ^[0-9a-fA-F]{64}$ ]] || { echo "Missing or invalid DISCORD_FORECAST_PUBLIC_KEY variable." >&2; exit 1; }

      - name: Install dependencies
        run: npm ci

      - name: Protect statistics D1 before Forecast migrations
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_D1_ANALYTICS_TOKEN: ${{ secrets.CLOUDFLARE_D1_ANALYTICS_TOKEN || secrets.CLOUDFLARE_API_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          stats_probe=$(npx wrangler d1 execute DB --remote \
            --config cloudflare/wrangler.toml --command "SELECT 1 AS ok;" --json)
          [[ "$(jq -r '.[0].results[0].ok' <<<"$stats_probe")" == "1" ]]
          npm run check:d1-budget -- preflight --output "$RUNNER_TEMP/d1-budget-preflight.json"

      - name: Verify collector
        run: |
          npm run forecast:types:check
          npx tsc --noEmit -p forecast-collector/tsconfig.json
          npm run test:forecast-collector
          npx wrangler deploy --dry-run --env staging --config forecast-collector/wrangler.toml

      - name: Verify dispatcher
        run: |
          npm run dispatcher:types:check
          npx tsc --noEmit -p forecast-dispatcher/tsconfig.json
          npm run test:forecast-dispatcher
          npx wrangler deploy --dry-run --env staging --config forecast-dispatcher/wrangler.toml

      - name: Verify interaction Router
        run: |
          npm run interactions:types:check
          npx tsc --noEmit -p forecast-interactions/tsconfig.json
          npm run test:forecast-interactions
          npx wrangler deploy --dry-run --config forecast-interactions/wrangler.toml

      - name: Apply staging Discord adoption migration
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: >-
          npx wrangler d1 execute FORECAST_DB --remote --env staging
          --config forecast-collector/wrangler.toml
          --file forecast-collector/migrations/0005_discord_staging_adoptions.sql

      - name: Apply staging Discord message identity migration
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          migration=$(npx wrangler d1 execute FORECAST_DB --remote --env staging \
            --config forecast-collector/wrangler.toml \
            --command "SELECT COUNT(*) AS count FROM schema_migrations WHERE version = 6;" \
            --json)
          [[ "$(jq -r '.[0].results[0].count' <<<"$migration")" == "1" ]] || \
            npx wrangler d1 execute FORECAST_DB --remote --env staging \
              --config forecast-collector/wrangler.toml \
              --file forecast-collector/migrations/0006_discord_staging_message_identity.sql

      - name: Apply staging workflow dispatch operations migration
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          migration=$(npx wrangler d1 execute FORECAST_DB --remote --env staging \
            --config forecast-collector/wrangler.toml \
            --command "SELECT COUNT(*) AS count FROM schema_migrations WHERE version = 7;" \
            --json)
          [[ "$(jq -r '.[0].results[0].count' <<<"$migration")" == "1" ]] || \
            npx wrangler d1 execute FORECAST_DB --remote --env staging \
              --config forecast-collector/wrangler.toml \
              --file forecast-collector/migrations/0007_workflow_dispatch_ops.sql

      - name: Apply staging manual review and Router audit migration
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          migration=$(npx wrangler d1 execute FORECAST_DB --remote --env staging \
            --config forecast-collector/wrangler.toml \
            --command "SELECT COUNT(*) AS count FROM schema_migrations WHERE version = 8;" \
            --json)
          [[ "$(jq -r '.[0].results[0].count' <<<"$migration")" == "1" ]] || \
            npx wrangler d1 execute FORECAST_DB --remote --env staging \
              --config forecast-collector/wrangler.toml \
              --file forecast-collector/migrations/0008_manual_reviews_interactions_canary.sql

      - name: Apply staging D1 budget and canary v6 migration
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          migration=$(npx wrangler d1 execute FORECAST_DB --remote --env staging \
            --config forecast-collector/wrangler.toml \
            --command "SELECT COUNT(*) AS count FROM schema_migrations WHERE version = 9;" \
            --json)
          [[ "$(jq -r '.[0].results[0].count' <<<"$migration")" == "1" ]] || \
            npx wrangler d1 execute FORECAST_DB --remote --env staging \
              --config forecast-collector/wrangler.toml \
              --file forecast-collector/migrations/0009_d1_budget_canary_v6.sql

      - name: Verify account Cron trigger capacity
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          FORECAST_DISPATCHER_SCRIPT_NAME: collection-kit-forecast-dispatcher-staging
        run: npm run check:cloudflare-cron-capacity

      - name: Prepare dispatcher secret file
        env:
          FORECAST_GITHUB_APP_PRIVATE_KEY: ${{ secrets.FORECAST_GITHUB_APP_PRIVATE_KEY }}
          DISCORD_BOT_TOKEN: ${{ secrets.DISCORD_FORECAST_BOT_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          node --input-type=module - "$RUNNER_TEMP/forecast-dispatcher-secrets.json" <<'NODE'
          import { writeFileSync } from "node:fs";
          const privateKey = process.env.FORECAST_GITHUB_APP_PRIVATE_KEY ?? "";
          const discordToken = process.env.DISCORD_BOT_TOKEN ?? "";
          if (!privateKey.includes("BEGIN") || !discordToken) throw new Error("Dispatcher secrets are invalid.");
          writeFileSync(
            process.argv[2],
            JSON.stringify({ GITHUB_APP_PRIVATE_KEY: privateKey, DISCORD_BOT_TOKEN: discordToken }),
            { mode: 0o600 },
          );
          NODE

      - name: Probe GitHub App installation and fixed workflow
        env:
          FORECAST_GITHUB_APP_ID: ${{ vars.FORECAST_GITHUB_APP_ID }}
          FORECAST_GITHUB_APP_INSTALLATION_ID: ${{ vars.FORECAST_GITHUB_APP_INSTALLATION_ID }}
          FORECAST_GITHUB_APP_PRIVATE_KEY: ${{ secrets.FORECAST_GITHUB_APP_PRIVATE_KEY }}
        run: npm run probe:forecast-github-app

      - name: Deploy staging dispatcher
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          FORECAST_GITHUB_APP_ID: ${{ vars.FORECAST_GITHUB_APP_ID }}
          FORECAST_GITHUB_APP_INSTALLATION_ID: ${{ vars.FORECAST_GITHUB_APP_INSTALLATION_ID }}
          DISCORD_FORECAST_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_CHANNEL_ID }}
          DISCORD_FORECAST_ACTIVITY_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_ACTIVITY_CHANNEL_ID }}
          DISCORD_FORECAST_ALERT_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_ALERT_CHANNEL_ID }}
          DISCORD_FORECAST_FALLBACK_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_FALLBACK_CHANNEL_ID }}
        run: >-
          npx wrangler deploy --env staging --config forecast-dispatcher/wrangler.toml
          --secrets-file "$RUNNER_TEMP/forecast-dispatcher-secrets.json"
          --var ENVIRONMENT:staging --var DEPLOY_SHA:${{ github.sha }}
          --var GITHUB_APP_ID:$FORECAST_GITHUB_APP_ID
          --var GITHUB_APP_INSTALLATION_ID:$FORECAST_GITHUB_APP_INSTALLATION_ID
          --var DISCORD_CHANNEL_ID:$DISCORD_FORECAST_CHANNEL_ID
          --var DISCORD_ACTIVITY_CHANNEL_ID:$DISCORD_FORECAST_ACTIVITY_CHANNEL_ID
          --var DISCORD_ALERT_CHANNEL_ID:$DISCORD_FORECAST_ALERT_CHANNEL_ID
          --var DISCORD_FALLBACK_CHANNEL_ID:$DISCORD_FORECAST_FALLBACK_CHANNEL_ID
          --var DISPATCH_ENABLED:false
          --tag "${{ github.sha }}-disabled" --message "Forecast dispatcher staging disabled ${{ github.sha }}"

      - name: Prepare deployment secret file
        env:
          FORECAST_COLLECTOR_ADMIN_TOKEN: ${{ secrets.FORECAST_COLLECTOR_ADMIN_TOKEN }}
          DISCORD_BOT_TOKEN: ${{ secrets.DISCORD_FORECAST_BOT_TOKEN }}
          DISCORD_FORECAST_APPLICATION_ID: ${{ vars.DISCORD_FORECAST_APPLICATION_ID }}
          DISCORD_FORECAST_PUBLIC_KEY: ${{ vars.DISCORD_FORECAST_PUBLIC_KEY }}
        shell: bash
        run: |
          set -euo pipefail
          test -n "$FORECAST_COLLECTOR_ADMIN_TOKEN" || {
            echo "Missing FORECAST_COLLECTOR_ADMIN_TOKEN." >&2
            exit 1
          }
          test -n "$DISCORD_BOT_TOKEN" || {
            echo "Missing DISCORD_FORECAST_BOT_TOKEN." >&2
            exit 1
          }
          application="$RUNNER_TEMP/discord-bot-application.json"
          status=$(curl --silent --show-error --max-time 10 \
            -H "Authorization: Bot $DISCORD_BOT_TOKEN" \
            --output "$application" --write-out "%{http_code}" \
            "https://discord.com/api/v10/oauth2/applications/@me")
          [[ "$status" == "200" ]] || {
            echo "Discord bot application lookup returned HTTP $status." >&2
            exit 1
          }
          node --input-type=module - "$application" "$RUNNER_TEMP/forecast-secrets.json" <<'NODE'
          import { readFileSync, writeFileSync } from "node:fs";
          const application = JSON.parse(readFileSync(process.argv[2], "utf8"));
          if (!/^\d{1,24}$/.test(application.id ?? "")) {
            throw new Error("Discord bot application ID is invalid.");
          }
          if (!/^[0-9a-f]{64}$/i.test(application.verify_key ?? "")) {
            throw new Error("Discord bot application verify key is invalid.");
          }
          if (application.id !== process.env.DISCORD_FORECAST_APPLICATION_ID) {
            throw new Error("Discord bot application ID does not match the repository variable.");
          }
          if (application.verify_key.toLowerCase() !== process.env.DISCORD_FORECAST_PUBLIC_KEY?.toLowerCase()) {
            throw new Error("Discord bot public key does not match the repository variable.");
          }
          const applicationName = String(application.name ?? "").trim();
          if (!applicationName || /[\r\n]/.test(applicationName)) {
            throw new Error("Discord bot application name is invalid.");
          }
          console.log(`Discord bot application: ${applicationName} (${application.id})`);
          writeFileSync(
            process.argv[3],
            JSON.stringify({
              ADMIN_TOKEN: process.env.FORECAST_COLLECTOR_ADMIN_TOKEN,
              DISCORD_APPLICATION_ID: application.id,
              DISCORD_PUBLIC_KEY: application.verify_key,
            }),
          );
          NODE

      - name: Deploy dedicated Discord interaction Router
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          DISCORD_FORECAST_GUILD_ID: ${{ vars.DISCORD_FORECAST_GUILD_ID }}
          DISCORD_FORECAST_APPROVER_USER_ID: ${{ vars.DISCORD_FORECAST_APPROVER_USER_ID }}
          DISCORD_FORECAST_APPROVAL_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_APPROVAL_CHANNEL_ID }}
          DISCORD_FORECAST_ALERT_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_ALERT_CHANNEL_ID }}
          DISCORD_FORECAST_ACTIVITY_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_ACTIVITY_CHANNEL_ID }}
        shell: bash
        run: |
          set -euo pipefail
          application="$RUNNER_TEMP/discord-bot-application.json"
          application_id=$(jq -r '.id' "$application")
          public_key=$(jq -r '.verify_key' "$application")
          npx wrangler deploy --config forecast-interactions/wrangler.toml \
            --var DEPLOY_SHA:${{ github.sha }} \
            --var DISCORD_APPLICATION_ID:$application_id \
            --var DISCORD_PUBLIC_KEY:$public_key \
            --var DISCORD_GUILD_ID:$DISCORD_FORECAST_GUILD_ID \
            --var DISCORD_APPROVER_USER_ID:$DISCORD_FORECAST_APPROVER_USER_ID \
            --var DISCORD_APPROVAL_CHANNEL_ID:$DISCORD_FORECAST_APPROVAL_CHANNEL_ID \
            --var DISCORD_ALERT_CHANNEL_ID:$DISCORD_FORECAST_ALERT_CHANNEL_ID \
            --var DISCORD_ACTIVITY_CHANNEL_ID:$DISCORD_FORECAST_ACTIVITY_CHANNEL_ID \
            --var PRODUCTION_MUTATIONS_ENABLED:false \
            --tag "${{ github.sha }}-staging" \
            --message "Forecast interaction Router staging ${{ github.sha }}"

      - name: Verify dedicated Router readiness
        if: vars.FORECAST_INTERACTIONS_URL != ''
        env:
          FORECAST_EXPECTED_DEPLOY_SHA: ${{ github.sha }}
          FORECAST_INTERACTIONS_URL: ${{ vars.FORECAST_INTERACTIONS_URL }}
        run: npm run smoke:forecast-interactions -- "$FORECAST_INTERACTIONS_URL"

      - name: Report Router bootstrap URL requirement
        if: vars.FORECAST_INTERACTIONS_URL == ''
        run: |
          {
            echo "::warning::The dedicated Router was deployed, but FORECAST_INTERACTIONS_URL is not configured."
            echo "Set FORECAST_INTERACTIONS_URL to the deployed Router origin, update the Discord Interaction Endpoint, then rerun this workflow with router_endpoint_ready=true."
          }

      - name: Deploy staging collector
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          DISCORD_FORECAST_APPROVAL_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_APPROVAL_CHANNEL_ID }}
          POLL_MODE: ${{ inputs.poll_mode || 'both' }}
          DISCORD_INTERACTION_OWNER: ${{ github.event_name == 'workflow_dispatch' && inputs.router_endpoint_ready && 'router' || 'collector' }}
        run: >-
          npx wrangler deploy --env staging --config forecast-collector/wrangler.toml
          --secrets-file "$RUNNER_TEMP/forecast-secrets.json"
          --var ENVIRONMENT:staging --var DEPLOY_SHA:${{ github.sha }} --var POLL_MODE:$POLL_MODE
          --var COLLECT_ENABLED:false
          --var DISCORD_CHANNEL_ID:$DISCORD_FORECAST_APPROVAL_CHANNEL_ID
          --var DISCORD_INTERACTION_OWNER:$DISCORD_INTERACTION_OWNER
          --tag "${{ github.sha }}-$POLL_MODE-disabled"
          --message "Forecast collector staging disabled ${{ github.sha }} ($POLL_MODE)"

      - name: Live staging queue round-trip smoke
        env:
          FORECAST_COLLECTOR_STAGING_URL: ${{ vars.FORECAST_COLLECTOR_STAGING_URL }}
          FORECAST_COLLECTOR_ADMIN_TOKEN: ${{ secrets.FORECAST_COLLECTOR_ADMIN_TOKEN }}
          FORECAST_EXPECTED_DEPLOY_SHA: ${{ github.sha }}
        run: npm run smoke:forecast-collector -- "$FORECAST_COLLECTOR_STAGING_URL" "$FORECAST_COLLECTOR_ADMIN_TOKEN"

      - name: Verify billing window and account-wide D1 baseline
        if: github.event_name == 'workflow_dispatch' && inputs.router_endpoint_ready
        id: d1_baseline
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_D1_ANALYTICS_TOKEN: ${{ secrets.CLOUDFLARE_D1_ANALYTICS_TOKEN || secrets.CLOUDFLARE_API_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          earliest_hour=$(TZ=Asia/Seoul date --date='+30 minutes' +%H)
          latest_hour=$(TZ=Asia/Seoul date --date='+45 minutes' +%H)
          [[ "$earliest_hour" == "11" && "$latest_hour" == "11" ]] || {
            echo "The 30-minute burn-in must finish between 11:00 and 11:59 KST." >&2
            exit 1
          }
          stats_probe=$(npx wrangler d1 execute DB --remote \
            --config cloudflare/wrangler.toml --command "SELECT 1 AS ok;" --json)
          [[ "$(jq -r '.[0].results[0].ok' <<<"$stats_probe")" == "1" ]] || {
            echo "Statistics production D1 read probe failed." >&2
            exit 1
          }
          npm run check:d1-budget -- snapshot --output "$RUNNER_TEMP/d1-budget-baseline.json"

      - name: Activate staging collector for D1 burn-in
        if: github.event_name == 'workflow_dispatch' && inputs.router_endpoint_ready
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          DISCORD_FORECAST_APPROVAL_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_APPROVAL_CHANNEL_ID }}
          POLL_MODE: ${{ inputs.poll_mode || 'both' }}
        run: >-
          npx wrangler deploy --env staging --config forecast-collector/wrangler.toml
          --secrets-file "$RUNNER_TEMP/forecast-secrets.json"
          --var ENVIRONMENT:staging --var DEPLOY_SHA:${{ github.sha }} --var POLL_MODE:$POLL_MODE
          --var COLLECT_ENABLED:true
          --var DISCORD_CHANNEL_ID:$DISCORD_FORECAST_APPROVAL_CHANNEL_ID
          --var DISCORD_INTERACTION_OWNER:router
          --tag "${{ github.sha }}-$POLL_MODE-burn-in"
          --message "Forecast collector staging D1 burn-in ${{ github.sha }} ($POLL_MODE)"

      - name: Activate staging dispatcher after collector readiness
        if: github.event_name == 'workflow_dispatch' && inputs.router_endpoint_ready
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          FORECAST_GITHUB_APP_ID: ${{ vars.FORECAST_GITHUB_APP_ID }}
          FORECAST_GITHUB_APP_INSTALLATION_ID: ${{ vars.FORECAST_GITHUB_APP_INSTALLATION_ID }}
          DISCORD_FORECAST_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_CHANNEL_ID }}
          DISCORD_FORECAST_ACTIVITY_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_ACTIVITY_CHANNEL_ID }}
          DISCORD_FORECAST_ALERT_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_ALERT_CHANNEL_ID }}
          DISCORD_FORECAST_FALLBACK_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_FALLBACK_CHANNEL_ID }}
        run: >-
          npx wrangler deploy --env staging --config forecast-dispatcher/wrangler.toml
          --secrets-file "$RUNNER_TEMP/forecast-dispatcher-secrets.json"
          --var ENVIRONMENT:staging --var DEPLOY_SHA:${{ github.sha }}
          --var GITHUB_APP_ID:$FORECAST_GITHUB_APP_ID
          --var GITHUB_APP_INSTALLATION_ID:$FORECAST_GITHUB_APP_INSTALLATION_ID
          --var DISCORD_CHANNEL_ID:$DISCORD_FORECAST_CHANNEL_ID
          --var DISCORD_ACTIVITY_CHANNEL_ID:$DISCORD_FORECAST_ACTIVITY_CHANNEL_ID
          --var DISCORD_ALERT_CHANNEL_ID:$DISCORD_FORECAST_ALERT_CHANNEL_ID
          --var DISCORD_FALLBACK_CHANNEL_ID:$DISCORD_FORECAST_FALLBACK_CHANNEL_ID
          --var DISPATCH_ENABLED:true
          --tag "${{ github.sha }}-enabled" --message "Forecast dispatcher staging enabled ${{ github.sha }}"

      - name: Live staging dispatcher callback and Discord smoke
        if: github.event_name == 'workflow_dispatch' && inputs.router_endpoint_ready
        id: dispatcher_smoke
        continue-on-error: true
        env:
          FORECAST_COLLECTOR_STAGING_URL: ${{ vars.FORECAST_COLLECTOR_STAGING_URL }}
          FORECAST_COLLECTOR_ADMIN_TOKEN: ${{ secrets.FORECAST_COLLECTOR_ADMIN_TOKEN }}
        run: >-
          npm run smoke:forecast-dispatcher --
          "$FORECAST_COLLECTOR_STAGING_URL"
          "$FORECAST_COLLECTOR_ADMIN_TOKEN"
          "deploy:${{ github.sha }}:${{ github.run_id }}"

      - name: Disable staging dispatcher after smoke failure
        if: github.event_name == 'workflow_dispatch' && inputs.router_endpoint_ready && steps.dispatcher_smoke.outcome == 'failure'
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          FORECAST_GITHUB_APP_ID: ${{ vars.FORECAST_GITHUB_APP_ID }}
          FORECAST_GITHUB_APP_INSTALLATION_ID: ${{ vars.FORECAST_GITHUB_APP_INSTALLATION_ID }}
          DISCORD_FORECAST_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_CHANNEL_ID }}
          DISCORD_FORECAST_ACTIVITY_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_ACTIVITY_CHANNEL_ID }}
          DISCORD_FORECAST_ALERT_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_ALERT_CHANNEL_ID }}
          DISCORD_FORECAST_FALLBACK_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_FALLBACK_CHANNEL_ID }}
        run: >-
          npx wrangler deploy --env staging --config forecast-dispatcher/wrangler.toml
          --secrets-file "$RUNNER_TEMP/forecast-dispatcher-secrets.json"
          --var ENVIRONMENT:staging --var DEPLOY_SHA:${{ github.sha }}
          --var GITHUB_APP_ID:$FORECAST_GITHUB_APP_ID
          --var GITHUB_APP_INSTALLATION_ID:$FORECAST_GITHUB_APP_INSTALLATION_ID
          --var DISCORD_CHANNEL_ID:$DISCORD_FORECAST_CHANNEL_ID
          --var DISCORD_ACTIVITY_CHANNEL_ID:$DISCORD_FORECAST_ACTIVITY_CHANNEL_ID
          --var DISCORD_ALERT_CHANNEL_ID:$DISCORD_FORECAST_ALERT_CHANNEL_ID
          --var DISCORD_FALLBACK_CHANNEL_ID:$DISCORD_FORECAST_FALLBACK_CHANNEL_ID
          --var DISPATCH_ENABLED:false
          --tag "${{ github.sha }}-disabled-after-smoke"
          --message "Disable forecast dispatcher after staging smoke failure ${{ github.sha }}"

      - name: Disable staging collector after dispatcher smoke failure
        if: github.event_name == 'workflow_dispatch' && inputs.router_endpoint_ready && steps.dispatcher_smoke.outcome == 'failure'
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          DISCORD_FORECAST_APPROVAL_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_APPROVAL_CHANNEL_ID }}
          POLL_MODE: ${{ inputs.poll_mode || 'both' }}
        run: >-
          npx wrangler deploy --env staging --config forecast-collector/wrangler.toml
          --secrets-file "$RUNNER_TEMP/forecast-secrets.json"
          --var ENVIRONMENT:staging --var DEPLOY_SHA:${{ github.sha }} --var POLL_MODE:$POLL_MODE
          --var COLLECT_ENABLED:false
          --var DISCORD_CHANNEL_ID:$DISCORD_FORECAST_APPROVAL_CHANNEL_ID
          --var DISCORD_INTERACTION_OWNER:router
          --tag "${{ github.sha }}-$POLL_MODE-disabled-after-smoke"
          --message "Disable forecast collector after staging smoke failure ${{ github.sha }}"

      - name: Fail after staging dispatcher rollback
        if: github.event_name == 'workflow_dispatch' && inputs.router_endpoint_ready && steps.dispatcher_smoke.outcome == 'failure'
        run: exit 1

      - name: Observe 30-minute account-wide D1 burn-in
        if: github.event_name == 'workflow_dispatch' && inputs.router_endpoint_ready && steps.dispatcher_smoke.outcome == 'success'
        run: sleep 1800

      - name: Evaluate account-wide D1 budget
        if: github.event_name == 'workflow_dispatch' && inputs.router_endpoint_ready && steps.dispatcher_smoke.outcome == 'success'
        id: d1_budget
        continue-on-error: true
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_D1_ANALYTICS_TOKEN: ${{ secrets.CLOUDFLARE_D1_ANALYTICS_TOKEN || secrets.CLOUDFLARE_API_TOKEN }}
        run: >-
          npm run check:d1-budget -- evaluate
          --baseline "$RUNNER_TEMP/d1-budget-baseline.json"
          --output "$RUNNER_TEMP/d1-budget-evaluation.json"

      - name: Disable staging Forecast automation after D1 budget failure
        if: always() && steps.d1_budget.outcome == 'failure'
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          FORECAST_GITHUB_APP_ID: ${{ vars.FORECAST_GITHUB_APP_ID }}
          FORECAST_GITHUB_APP_INSTALLATION_ID: ${{ vars.FORECAST_GITHUB_APP_INSTALLATION_ID }}
          DISCORD_FORECAST_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_CHANNEL_ID }}
          DISCORD_FORECAST_ACTIVITY_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_ACTIVITY_CHANNEL_ID }}
          DISCORD_FORECAST_ALERT_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_ALERT_CHANNEL_ID }}
          DISCORD_FORECAST_FALLBACK_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_FALLBACK_CHANNEL_ID }}
          DISCORD_FORECAST_APPROVAL_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_APPROVAL_CHANNEL_ID }}
          POLL_MODE: ${{ inputs.poll_mode || 'both' }}
        shell: bash
        run: |
          set -euo pipefail
          npx wrangler deploy --env staging --config forecast-dispatcher/wrangler.toml \
            --secrets-file "$RUNNER_TEMP/forecast-dispatcher-secrets.json" \
            --var ENVIRONMENT:staging --var DEPLOY_SHA:${{ github.sha }} \
            --var GITHUB_APP_ID:$FORECAST_GITHUB_APP_ID \
            --var GITHUB_APP_INSTALLATION_ID:$FORECAST_GITHUB_APP_INSTALLATION_ID \
            --var DISCORD_CHANNEL_ID:$DISCORD_FORECAST_CHANNEL_ID \
            --var DISCORD_ACTIVITY_CHANNEL_ID:$DISCORD_FORECAST_ACTIVITY_CHANNEL_ID \
            --var DISCORD_ALERT_CHANNEL_ID:$DISCORD_FORECAST_ALERT_CHANNEL_ID \
            --var DISCORD_FALLBACK_CHANNEL_ID:$DISCORD_FORECAST_FALLBACK_CHANNEL_ID \
            --var DISPATCH_ENABLED:false \
            --tag "${{ github.sha }}-d1-budget-stop" \
            --message "Disable forecast dispatcher after D1 budget failure ${{ github.sha }}"
          npx wrangler deploy --env staging --config forecast-collector/wrangler.toml \
            --secrets-file "$RUNNER_TEMP/forecast-secrets.json" \
            --var ENVIRONMENT:staging --var DEPLOY_SHA:${{ github.sha }} --var POLL_MODE:$POLL_MODE \
            --var COLLECT_ENABLED:false \
            --var DISCORD_CHANNEL_ID:$DISCORD_FORECAST_APPROVAL_CHANNEL_ID \
            --var DISCORD_INTERACTION_OWNER:router \
            --tag "${{ github.sha }}-$POLL_MODE-d1-budget-stop" \
            --message "Disable forecast collector after D1 budget failure ${{ github.sha }}"

      - name: Send direct Discord D1 budget failure alert
        if: always() && steps.d1_budget.outcome == 'failure'
        env:
          DISCORD_BOT_TOKEN: ${{ secrets.DISCORD_FORECAST_BOT_TOKEN }}
          DISCORD_ALERT_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_ALERT_CHANNEL_ID }}
        shell: bash
        run: |
          set -euo pipefail
          reasons=$(jq -r '.reasons | join(", ")' "$RUNNER_TEMP/d1-budget-evaluation.json" 2>/dev/null || echo "budget_evaluation_failed")
          payload=$(jq -n \
            --arg content "Forecast staging D1 canary was stopped. Reasons: $reasons. Run: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}" \
            '{content:$content,allowed_mentions:{parse:[]}}')
          curl --silent --show-error --fail-with-body --connect-timeout 5 --max-time 20 \
            -H "Authorization: Bot $DISCORD_BOT_TOKEN" \
            -H "Content-Type: application/json" \
            --data "$payload" \
            "https://discord.com/api/v10/channels/$DISCORD_ALERT_CHANNEL_ID/messages" >/dev/null

      - name: Fail after D1 budget rollback
        if: always() && steps.d1_budget.outcome == 'failure'
        run: exit 1

      - name: Start fresh eight-hour canary
        if: github.event_name == 'workflow_dispatch' && inputs.router_endpoint_ready && steps.d1_budget.outcome == 'success'
        id: canary_start
        env:
          FORECAST_COLLECTOR_STAGING_URL: ${{ vars.FORECAST_COLLECTOR_STAGING_URL }}
          FORECAST_COLLECTOR_ADMIN_TOKEN: ${{ secrets.FORECAST_COLLECTOR_ADMIN_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          canary_id="fc-$(node --input-type=module -e 'console.log(crypto.randomUUID().replaceAll("-", ""))')"
          jq -n \
            --arg canaryId "$canary_id" \
            --slurpfile evaluation "$RUNNER_TEMP/d1-budget-evaluation.json" \
            '{canaryId:$canaryId,collectorCron:"*/3 * * * *",dispatcherCron:"1-59/3 * * * *",quotaEvidence:$evaluation[0].evidence}' \
            > "$RUNNER_TEMP/canary-start-request.json"
          status=$(curl --silent --show-error --connect-timeout 5 --max-time 20 \
            --output "$RUNNER_TEMP/canary-start.json" --write-out "%{http_code}" \
            -H "Authorization: Bearer $FORECAST_COLLECTOR_ADMIN_TOKEN" \
            -H "Content-Type: application/json" \
            --data-binary "@$RUNNER_TEMP/canary-start-request.json" \
            "$FORECAST_COLLECTOR_STAGING_URL/admin/canary-deployments/start")
          [[ "$status" == "200" ]] || {
            echo "Canary start returned HTTP $status." >&2
            exit 1
          }
          jq -e --arg sha "${{ github.sha }}" --arg canaryId "$canary_id" \
            '.canary.canaryId == $canaryId and .canary.deploymentSha == $sha and .canary.collectorCron == "*/3 * * * *" and .canary.dispatcherCron == "1-59/3 * * * *"' \
            "$RUNNER_TEMP/canary-start.json" >/dev/null
          echo "canary_id=$canary_id" >> "$GITHUB_OUTPUT"

      - name: Send mutation-free Router test card
        if: github.event_name == 'workflow_dispatch' && inputs.router_endpoint_ready
        env:
          DISCORD_BOT_TOKEN: ${{ secrets.DISCORD_FORECAST_BOT_TOKEN }}
          DISCORD_ACTIVITY_CHANNEL_ID: ${{ vars.DISCORD_FORECAST_ACTIVITY_CHANNEL_ID }}
        run: npm run send:forecast-router-test

      - name: Record shadow start
        run: |
          {
            echo "### Forecast collector staging shadow"
            echo "- Commit: \`${{ github.sha }}\`"
            echo "- Cron: every 3 minutes"
            echo "- Dispatcher Cron: minute 1, then every 3 minutes"
            if [[ "${{ github.event_name == 'workflow_dispatch' && inputs.router_endpoint_ready }}" == "true" ]]; then
              echo "- Canary: fresh v6 eight-hour window started after a 30-minute account-wide D1 burn-in"
              echo "- Canary ID: `${{ steps.canary_start.outputs.canary_id }}`"
              echo "- D1 policy: all account databases included; statistics production reserve protected"
              echo "- Required action: click the Router response test card during this canary window"
              echo "- Promotion: blocked until the authenticated v6 report and final D1 budget check pass"
            else
              echo "- Dispatcher: remains disabled"
              echo "- Canary: not started"
              if [[ -n "${{ vars.FORECAST_INTERACTIONS_URL }}" ]]; then
                echo "- Required action: set the Discord Interaction Endpoint to `${{ vars.FORECAST_INTERACTIONS_URL }}/discord/interactions`, then dispatch this workflow with router_endpoint_ready=true"
              else
                echo "- Required action: read the dedicated Router URL from the Wrangler deployment output, set FORECAST_INTERACTIONS_URL to that origin, update the Discord Interaction Endpoint, then dispatch this workflow with router_endpoint_ready=true"
              fi
            fi
          } >> "$GITHUB_STEP_SUMMARY"
