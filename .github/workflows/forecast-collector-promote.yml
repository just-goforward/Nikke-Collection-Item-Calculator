name: Promote Forecast Collector

on:
  workflow_dispatch:

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  group: forecast-collector-production
  cancel-in-progress: false

jobs:
  canary:
    if: vars.FORECAST_COLLECTOR_STAGING_URL != ''
    runs-on: ubuntu-latest
    outputs:
      canary_passed: ${{ steps.canary.outputs.canary_passed }}
      deployment_sha: ${{ steps.canary.outputs.deployment_sha }}
      canary_early_failed: ${{ steps.canary.outputs.canary_early_failed }}
      poll_mode: ${{ steps.canary.outputs.poll_mode }}
    steps:
      - name: Checkout trusted main
        uses: actions/checkout@v7
        with:
          ref: refs/heads/main
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Read authenticated 12-hour canary report
        id: canary
        env:
          FORECAST_COLLECTOR_URL: ${{ vars.FORECAST_COLLECTOR_STAGING_URL }}
          FORECAST_COLLECTOR_ADMIN_TOKEN: ${{ secrets.FORECAST_COLLECTOR_ADMIN_TOKEN }}
        run: npm run check:forecast-canary

      - name: Verify canary deployment inputs match current main
        if: steps.canary.outputs.canary_passed == 'true'
        env:
          CANARY_SHA: ${{ steps.canary.outputs.deployment_sha }}
        shell: bash
        run: |
          [[ "$CANARY_SHA" =~ ^[0-9a-f]{40}$ ]] || {
            echo "Staging canary returned an invalid deployment SHA." >&2
            exit 1
          }
          git cat-file -e "$CANARY_SHA^{commit}" || {
            echo "Staging canary commit $CANARY_SHA is not in repository history." >&2
            exit 1
          }
          git diff --quiet "$CANARY_SHA" HEAD -- \
            forecast-collector \
            ':(exclude)forecast-collector/README.md' \
            ':(exclude)forecast-collector/src/candidate.ts' \
            ':(exclude)forecast-collector/src/candidate.test.ts' \
            shared/supplyForecastCandidate.ts \
            shared/supplyForecastModel.ts \
            scripts/smoke-forecast-collector.ts \
            vitest.forecast-collector.config.ts \
            .github/workflows/forecast-collector-deploy.yml \
            package.json \
            package-lock.json || {
            echo "Forecast collector deployment inputs changed after canary start." >&2
            exit 1
          }

      - name: Canary pending summary
        if: steps.canary.outputs.canary_passed != 'true'
        run: |
          echo "12-hour forecast collector canary is not yet eligible or has not passed." >> "$GITHUB_STEP_SUMMARY"
          if [[ "${{ steps.canary.outputs.canary_early_failed }}" == "true" && "${{ steps.canary.outputs.poll_mode }}" == "both" ]]; then
            echo "More than 1% of invocations were abandoned after two hours. Dispatch the staging workflow with poll_mode=alternating; do not reuse this window." >> "$GITHUB_STEP_SUMMARY"
          fi

  promote:
    needs: canary
    if: needs.canary.outputs.canary_passed == 'true'
    runs-on: ubuntu-latest
    environment: cloudflare-production
    steps:
      - name: Checkout trusted main
        uses: actions/checkout@v7
        with:
          ref: refs/heads/main

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Validate production configuration
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          FORECAST_COLLECTOR_ADMIN_TOKEN: ${{ secrets.FORECAST_COLLECTOR_ADMIN_TOKEN }}
          FORECAST_COLLECTOR_PRODUCTION_URL: ${{ vars.FORECAST_COLLECTOR_PRODUCTION_URL }}
        run: |
          test -n "$CLOUDFLARE_ACCOUNT_ID" && test -n "$CLOUDFLARE_API_TOKEN" && \
          test -n "$FORECAST_COLLECTOR_ADMIN_TOKEN" && test -n "$FORECAST_COLLECTOR_PRODUCTION_URL"

      - name: Check whether this commit is already deployed
        id: deployed
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          DEPLOY_SHA: ${{ needs.canary.outputs.deployment_sha }}
          POLL_MODE: ${{ needs.canary.outputs.poll_mode }}
        shell: bash
        run: |
          set -euo pipefail
          versions="$RUNNER_TEMP/forecast-versions.json"
          error_log="$RUNNER_TEMP/forecast-versions.err"
          if NO_COLOR=1 npx wrangler versions list --env="" \
            --config forecast-collector/wrangler.toml --json > "$versions" 2> "$error_log"; then
            echo "worker_exists=true" >> "$GITHUB_OUTPUT"
          elif grep -q "This Worker does not exist on your account" "$error_log" && \
            grep -q "code: 10007" "$error_log"; then
            echo "worker_exists=false" >> "$GITHUB_OUTPUT"
            echo "needed=true" >> "$GITHUB_OUTPUT"
            echo "Production collector does not exist yet; continuing with its first deployment."
            exit 0
          else
            cat "$error_log" >&2
            exit 1
          fi
          if node -e 'const rows=require(process.argv[1]);process.exit(rows.some(x=>x.annotations?.["workers/tag"]===process.argv[2])?0:1)' "$versions" "$DEPLOY_SHA-$POLL_MODE"; then
            echo "needed=false" >> "$GITHUB_OUTPUT"
          else
            echo "needed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Prepare deployment secret file
        if: steps.deployed.outputs.needed == 'true'
        env:
          FORECAST_COLLECTOR_ADMIN_TOKEN: ${{ secrets.FORECAST_COLLECTOR_ADMIN_TOKEN }}
        run: >-
          node -e 'require("node:fs").writeFileSync(process.env.RUNNER_TEMP+"/forecast-secrets.json",JSON.stringify({ADMIN_TOKEN:process.env.FORECAST_COLLECTOR_ADMIN_TOKEN}))'

      - name: Capture production version
        if: steps.deployed.outputs.needed == 'true' && steps.deployed.outputs.worker_exists == 'true'
        id: previous
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        shell: bash
        run: |
          npx wrangler deployments status --env="" --config forecast-collector/wrangler.toml --json > "$RUNNER_TEMP/forecast-before.json"
          version=$(node -e 'const x=require(process.argv[1]);process.stdout.write(x.versions?.find(v=>v.percentage===100)?.version_id??"")' "$RUNNER_TEMP/forecast-before.json")
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Deploy production collector
        if: steps.deployed.outputs.needed == 'true'
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          DEPLOY_SHA: ${{ needs.canary.outputs.deployment_sha }}
          POLL_MODE: ${{ needs.canary.outputs.poll_mode }}
        run: >-
          npx wrangler deploy --env="" --config forecast-collector/wrangler.toml
          --secrets-file "$RUNNER_TEMP/forecast-secrets.json"
          --var ENVIRONMENT:production --var DEPLOY_SHA:$DEPLOY_SHA --var POLL_MODE:$POLL_MODE
          --tag "$DEPLOY_SHA-$POLL_MODE" --message "Forecast collector production $DEPLOY_SHA ($POLL_MODE)"

      - name: Wait for production health readiness
        env:
          FORECAST_COLLECTOR_PRODUCTION_URL: ${{ vars.FORECAST_COLLECTOR_PRODUCTION_URL }}
        run: |
          node --input-type=module - <<'NODE'
          const baseUrl = process.env.FORECAST_COLLECTOR_PRODUCTION_URL.replace(/\/$/, "");
          let lastError = "health endpoint did not become ready";
          let ready = false;
          for (let attempt = 1; attempt <= 12; attempt += 1) {
            try {
              const response = await fetch(`${baseUrl}/health`, {
                headers: { accept: "application/json" },
                signal: AbortSignal.timeout(10_000),
              });
              if (response.ok) {
                const body = await response.json();
                if (body?.status === "ok") {
                  ready = true;
                  break;
                }
                lastError = "health response did not contain status=ok";
              } else {
                lastError = `health returned ${response.status}`;
              }
            } catch (error) {
              lastError = error instanceof Error ? error.message : String(error);
            }
            if (attempt < 12) await new Promise((resolve) => setTimeout(resolve, 5_000));
          }
          if (!ready) throw new Error(`Production collector readiness failed: ${lastError}`);
          NODE

      - name: Production round-trip smoke
        id: smoke
        continue-on-error: true
        env:
          FORECAST_COLLECTOR_PRODUCTION_URL: ${{ vars.FORECAST_COLLECTOR_PRODUCTION_URL }}
          FORECAST_COLLECTOR_ADMIN_TOKEN: ${{ secrets.FORECAST_COLLECTOR_ADMIN_TOKEN }}
          FORECAST_EXPECTED_DEPLOY_SHA: ${{ needs.canary.outputs.deployment_sha }}
        run: npm run smoke:forecast-collector -- "$FORECAST_COLLECTOR_PRODUCTION_URL" "$FORECAST_COLLECTOR_ADMIN_TOKEN"

      - name: Bootstrap recent Solo Raid ledger
        if: steps.smoke.outcome == 'success'
        id: bootstrap
        continue-on-error: true
        env:
          FORECAST_COLLECTOR_URL: ${{ vars.FORECAST_COLLECTOR_PRODUCTION_URL }}
          FORECAST_COLLECTOR_ADMIN_TOKEN: ${{ secrets.FORECAST_COLLECTOR_ADMIN_TOKEN }}
          FORECAST_BOOTSTRAP_SOLO_HISTORY: "true"
        run: npm run process:forecast-source-queue

      - name: Restore production after smoke failure
        if: steps.deployed.outputs.needed == 'true' && (steps.smoke.outcome == 'failure' || steps.bootstrap.outcome == 'failure') && steps.previous.outputs.version != ''
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          PREVIOUS_VERSION: ${{ steps.previous.outputs.version }}
        run: >-
          npx wrangler versions deploy "$PREVIOUS_VERSION@100%" --yes --env=""
          --config forecast-collector/wrangler.toml --message "Restore after forecast smoke failure"

      - name: Fail after restore
        if: steps.smoke.outcome == 'failure' || steps.bootstrap.outcome == 'failure'
        run: exit 1

      - name: Verify proposal workflow targets production collector
        if: steps.smoke.outcome == 'success' && steps.bootstrap.outcome == 'success'
        env:
          FORECAST_COLLECTOR_URL: ${{ vars.FORECAST_COLLECTOR_URL }}
          FORECAST_COLLECTOR_PRODUCTION_URL: ${{ vars.FORECAST_COLLECTOR_PRODUCTION_URL }}
        run: |
          test -n "$FORECAST_COLLECTOR_URL" || {
            echo "Missing administrator-managed FORECAST_COLLECTOR_URL repository variable." >&2
            exit 1
          }
          test "$FORECAST_COLLECTOR_URL" = "$FORECAST_COLLECTOR_PRODUCTION_URL" || {
            echo "FORECAST_COLLECTOR_URL does not match the verified production collector URL." >&2
            exit 1
          }
