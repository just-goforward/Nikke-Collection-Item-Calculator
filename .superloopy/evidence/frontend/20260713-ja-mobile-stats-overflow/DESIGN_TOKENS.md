# Design Token Contract

This focused fix uses the existing project contract in `DESIGN.md` without adding tokens.

- Surface, border, text, and accent colors remain unchanged.
- Existing mobile and tablet breakpoints remain unchanged.
- Existing stats typography, spacing, card radius, and density remain unchanged.
- The implementation only corrects CSS Grid minimum sizing and emergency wrapping.
