# Design Token Contract

This runtime and accessibility hardening pass uses the existing `DESIGN.md` contract without adding visual tokens.

- No component color, spacing, typography, radius, shadow, or motion value changed.
- The stale-result overlay and status message retain their existing visual treatment.
- The result content now uses the platform `inert` behavior while stale.
