{
  "name": "@collection-kit/forecast-interactions",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --config vitest.config.ts",
    "test:coverage": "vitest run --config vitest.config.ts --coverage"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "1.1.4",
    "@vitest/coverage-v8": "5.0.0",
    "vitest": "4.1.11"
  }
}
